apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: ec2-obs-otlp-testes-location
  description: Location to import all ec2-obs-otlp-testes catalog components
spec:
  targets:
    - ./.platform/*.yaml
